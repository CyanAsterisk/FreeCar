spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: YAML
        prefix: freecar
        default-context: java
        data-key: trade_srv
        watch:
          enabled: true
      discovery:
        port: ${server.port}
        service-name: ${spring.application.name}