spring:
  application:
    name: trade_srv
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: YAML
        prefix: freecar
        default-context: java
        data-key: trade_srv
        watch:
          enabled: true
      discovery:
        enabled: true
        service-name: ${spring.application.name}
        port: ${thrift.trade.port}
        metadata:
          thrift-ports: ${thrift.trade.port}
        tags:
          - rpc-service
management:
  endpoint:
    health:
      show-details: always